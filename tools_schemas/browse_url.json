{
  "name": "browse_url",
  "description": "Visits a specific web address (URL) and returns the text content converted into clean Markdown. Useful for reading documentation, articles, or online code specifications.",
  "requiredTier": "small",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The full URL to visit (e.g. https://nodejs.org/en/about)."
      }
    },
    "required": ["url"]
  }
}
