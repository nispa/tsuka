{
  "name": "execute_command",
  "description": "Executes an arbitrary command or PowerShell script on Windows. Streams stdout/stderr live to the user's console. This tool allows full system modification and is dangerous.",
  "requiredTier": "medium",
  "parameters": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "description": "The command or PowerShell script to execute."
      },
      "timeout_ms": {
        "type": "integer",
        "description": "Maximum timeout in milliseconds (optional, e.g. 300000 for long builds or installs; default 120000)."
      }
    },
    "required": ["command"]
  }
}
