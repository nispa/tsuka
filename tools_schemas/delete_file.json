{
  "name": "delete_file",
  "description": "Permanently removes a file from the file system.",
  "requiredTier": "small",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "The path of the file to delete."
      }
    },
    "required": ["path"]
  }
}
