{
  "name": "recall_memory",
  "description": "Searches persistent shared memory for previously saved facts (from any agent, in any session). Without parameters it shows the most recent memories; with 'query' it searches by keyword.",
  "requiredTier": "small",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Keywords to search for in memory (optional; if omitted, shows the most recent)."
      },
      "limit": {
        "type": "integer",
        "description": "Maximum number of memories to return (optional, default 10, max 50)."
      }
    },
    "required": []
  }
}
