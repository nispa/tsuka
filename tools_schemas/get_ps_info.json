{
  "name": "get_ps_info",
  "description": "Retrieves read-only system information from Windows via built-in PowerShell cmdlets (running processes, active services, disk volumes, environment variables).",
  "requiredTier": "small",
  "parameters": {
    "type": "object",
    "properties": {
      "category": {
        "type": "string",
        "enum": ["processes", "services", "disk", "env"],
        "description": "The category of system information to collect."
      }
    },
    "required": ["category"]
  }
}
