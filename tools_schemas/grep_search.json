{
  "name": "grep_search",
  "description": "Recursively searches every text file in the workspace for lines matching a specific query.",
  "requiredTier": "small",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The term or phrase to search for."
      },
      "path": {
        "type": "string",
        "description": "Starting directory for the search (optional, defaults to the current folder)."
      }
    },
    "required": ["query"]
  }
}
