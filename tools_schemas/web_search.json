{
  "name": "web_search",
  "description": "Performs an internet search to gather real-time information and up-to-date answers on news, documentation, or code.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search phrase or term to look up on the web."
      }
    },
    "required": ["query"]
  }
}
