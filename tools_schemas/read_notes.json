{
  "name": "read_notes",
  "description": "Reads the notes colleagues left on the current run's shared blackboard (team or goal). Use it at the start of your turn to see decisions, artifacts, or open points from earlier colleagues in THIS run.",
  "requiredTier": "small",
  "riskLevel": "SAFE",
  "parameters": {
    "type": "object",
    "properties": {
      "prefix": {
        "type": "string",
        "description": "If specified, only shows notes whose key starts with this prefix (case-insensitive). Omitted: shows every note in the run."
      }
    },
    "required": []
  }
}
