{
  "name": "list_dir",
  "description": "Lists the contents of a directory (files and folders) in structured form, excluding hidden or heavy folders such as .git and node_modules.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "The path of the folder to list (optional, defaults to the current folder)."
      }
    }
  }
}
